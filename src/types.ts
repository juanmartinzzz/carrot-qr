export interface SignupFormData {
  businessName: string;
  location: string;
  contactInfo: string;
}

export interface FeatureStep {
  id: number;
  title: string;
  description: string;
  icon: string;
}